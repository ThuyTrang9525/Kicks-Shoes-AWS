{
  "name": "bedrock-chat-lambda",
  "version": "1.0.0",
  "description": "Lambda function for processing chat messages with AWS Bedrock",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "npm install && zip -r ../bedrock-chat.zip .",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-agent-runtime": "^3.700.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
